const ERORR_MESSAGES={
  emailFormat:'Invalid email format!',
  passwordFormat:'Password must be 8 characters long!',
  serverError:'Internal Server Error!',
  confirmPassword:'Passwords dont match!',
  required:'required field!'
}

const TOKEN_KEY='simplilearn-auth-module'

export {
  ERORR_MESSAGES,
  TOKEN_KEY,
}